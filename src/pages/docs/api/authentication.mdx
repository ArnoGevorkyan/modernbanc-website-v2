import { Table, Image } from '~/views/docs/components/content/all'

# Authentication

We use bearer tokens to authenticate API requests. Just add your API key in the header for Authorization key in a following way:

```shell
curl --url https://api.modernbanc.com -H 'Authorization: ApiKey YOUR_API_KEY'
```
